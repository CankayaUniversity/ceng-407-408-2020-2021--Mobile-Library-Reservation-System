<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mlrs2">

    <application
        android:allowBackup="true"
        android:icon="@drawable/lib_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/lib_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MLRS2">
        <activity android:name=".FirstPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".RegisterActivity"></activity>

        <activity android:name=".LM_MenuActivity"></activity>
        <activity android:name=".LM_ListLibraries_Activity"></activity>
        <activity android:name=".LM_ListReservations_Activity"></activity>

        <activity android:name=".Admin_AddLibrary_Activity"></activity>
        <activity android:name=".Admin_ListLibraries_Activity"></activity>

        <activity android:name=".Librarian_ListReservations_Activity"></activity>

    </application>

</manifest>